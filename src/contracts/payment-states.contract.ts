export enum PaymentStatus {
    authorized = 'authorized',
    settled = 'settled',
    successful = 'successful',
    approved = 'approved',
    canceled = 'canceled',
    failed = 'failed',
    refunded = 'refunded',
    pending = 'pending',
}
